<UserControl x:Class="StencilPad.UI.Widgets.ColorFieldSVPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="160">
    <Grid ClipToBounds="True">
        <!-- White-to-hue gradient (saturation axis) -->
        <Rectangle x:Name="SaturationRect">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                    <GradientStop Color="White" Offset="0"/>
                    <GradientStop Color="Red" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Transparent-to-black overlay (brightness axis) -->
        <Rectangle>
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="Transparent" Offset="0"/>
                    <GradientStop Color="Black" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Drag canvas with crosshair marker -->
        <Canvas x:Name="DragCanvas" Background="Transparent"
                MouseDown="DragCanvas_MouseDown"
                MouseMove="DragCanvas_MouseMove"
                MouseUp="DragCanvas_MouseUp">
            <Ellipse x:Name="MarkerOuter"
                     Width="12" Height="12"
                     Stroke="Black" StrokeThickness="2"
                     Fill="Transparent" Opacity="0.5"
                     IsHitTestVisible="False"/>
            <Ellipse x:Name="Marker"
                     Width="10" Height="10"
                     Stroke="White" StrokeThickness="2"
                     Fill="Transparent"
                     IsHitTestVisible="False"/>
        </Canvas>
    </Grid>
</UserControl>
