<UserControl x:Class="StencilPad.UI.Widgets.UnitField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:StencilPad.UI.Widgets"
             xmlns:sp="clr-namespace:StencilPad.Spatial"
             x:Name="Root">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="ValueField"
                 KeyDown="ValueField_KeyDown"
                 Grid.Column="0" 
                 VerticalContentAlignment="Center"
                 Padding="4,2"/>

        <ComboBox Grid.Column="1" 
                  Margin="4,0,0,0"
                  SelectedValue="{Binding UnitType, ElementName=Root}"
                  SelectedValuePath="Value"
                  DisplayMemberPath="Description"
                  MinWidth="60">
            <ComboBox.Items>
                <local:UnitItem Value="{x:Static sp:UnitType.Millimeters}" Description="mm"/>
                <local:UnitItem Value="{x:Static sp:UnitType.Inches}" Description="in"/>
            </ComboBox.Items>
        </ComboBox>
    </Grid>
</UserControl>
