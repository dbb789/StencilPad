<widgets:ColorFieldSliderBase x:Class="StencilPad.UI.Widgets.ColorFieldAlphaSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:widgets="clr-namespace:StencilPad.UI.Widgets">
    <Grid ClipToBounds="True">
        <!-- Checkerboard background -->
        <Rectangle>
            <Rectangle.Fill>
                <DrawingBrush TileMode="Tile" Viewport="0,0,8,8" ViewportUnits="Absolute">
                    <DrawingBrush.Drawing>
                        <DrawingGroup>
                            <GeometryDrawing Brush="#FFCCCCCC">
                                <GeometryDrawing.Geometry>
                                    <RectangleGeometry Rect="0,0,8,8"/>
                                </GeometryDrawing.Geometry>
                            </GeometryDrawing>
                            <GeometryDrawing Brush="#FFFFFFFF">
                                <GeometryDrawing.Geometry>
                                    <GeometryGroup>
                                        <RectangleGeometry Rect="0,0,4,4"/>
                                        <RectangleGeometry Rect="4,4,4,4"/>
                                    </GeometryGroup>
                                </GeometryDrawing.Geometry>
                            </GeometryDrawing>
                        </DrawingGroup>
                    </DrawingBrush.Drawing>
                </DrawingBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Transparent-to-colour gradient -->
        <Rectangle x:Name="GradientRect"/>

        <!-- Drag canvas with marker -->
        <Canvas x:Name="DragCanvas" Background="Transparent">
            <Rectangle x:Name="Marker"
                       Width="4" Height="20"
                       Stroke="Black" StrokeThickness="1"
                       Fill="White" Opacity="0.8"
                       IsHitTestVisible="False"/>
        </Canvas>
    </Grid>
</widgets:ColorFieldSliderBase>

